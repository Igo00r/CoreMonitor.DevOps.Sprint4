{
  "name": "coremonitor-api",
  "version": "1.0.0",
  "description": "API REST do CoreMonitor - modulo de Clientes e Vendas (Projeto Integrador FIAP - DevOps)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "test": "jest --runInBand --forceExit",
    "lint": "echo \"Lint OK (placeholder para a pipeline)\" && exit 0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
